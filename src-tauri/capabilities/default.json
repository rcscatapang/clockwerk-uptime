{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: core APIs plus the file-open dialog used to pick a monitor sync file. All network, fs, and OS access happens in Rust; the dialog returns a path, never file contents.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
