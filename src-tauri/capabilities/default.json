{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: core APIs only. All network, fs, and OS access happens in Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
